# 记忆模块（mem0 长期记忆 + 自研工作记忆压缩）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 mem0 实现用户偏好层(用户级)与方法规范层(行业级，来自 Critic 反馈)两层长期记忆，并自研 Chat 上下文滚动压缩；接入 DeepResearch(Planner/Writer) 与 Chat，移除旧 `LongTermMemory`/`memory_service` 遗留实现。

**Architecture:** 新建 `MemoryEngine`(mem0 封装，懒加载单例) + `ContextCompressor`(自研滚动摘要)。长期记忆走 mem0(DashScope LLM/embedder + 复用 Milvus 集合 `mem0_memories`，dim 1024)；偏好层 scope=`user_id`，方法规范层 scope=`sop::{industry}`。所有记忆读写 best-effort、降级不抛、写走后台任务。

**Tech Stack:** Python 3.12 / FastAPI / SQLAlchemy(create_all，无 Alembic) / mem0ai / Milvus(pymilvus) / DashScope(OpenAI 兼容) / pytest 9。

---

## 约定（所有任务通用）

- **工作目录**：所有命令在 `backend/` 下执行（`cd backend`）。测试用 `from app.xxx import ...`。
- **commit 规范**（遵循 `CLAUDE.local.md`）：中文 commit message，**不要** co-author 行。
- **测试运行**：`python -m pytest <路径> -q -p no:cacheprovider`。
- **降级铁律**：`MemoryEngine` / `ContextCompressor` 的任何方法都不得让主流程抛错——读失败返回 `""`，写失败静默。

---

## 文件结构

**新建：**
- `backend/app/service/memory_engine.py` — `MemoryEngine`(mem0 封装) + `classify_industry()` + `get_memory_engine()` 单例
- `backend/app/service/context_compressor.py` — `ContextCompressor`
- `backend/app/scripts/sync_memory_schema.py` — 一次性 DB 同步(加列 / 删旧表)
- `backend/app/scripts/demo_memory.py` — 演示脚本
- `backend/test/test_memory/__init__.py`
- `backend/test/test_memory/test_classify_industry.py`
- `backend/test/test_memory/test_memory_engine.py`（mock mem0）
- `backend/test/test_memory/test_context_compressor.py`
- `backend/test/test_memory/test_memory_engine_smoke.py`（真 mem0+Milvus，标记 slow）

**修改：**
- `backend/requirements.txt` — 加 `mem0ai`(锁版本)
- `backend/app/service/deep_research_v2/state.py` — `ResearchState` 加 `user_id`
- `backend/app/service/deep_research_v2/graph.py` — 透传 user_id + 完成时写记忆
- `backend/app/service/deep_research_v2/agents/planner.py` — 注入偏好+教训
- `backend/app/service/deep_research_v2/agents/writer.py` — 注入偏好
- `backend/app/service/chat_service.py` — 替换记忆上下文 + 历史压缩
- `backend/app/models/chat.py` — 删 `LongTermMemory` + `ChatSession.memories`；`ChatSession` 加 `context_summary`/`summarized_up_to`
- `backend/app/models/user.py` — 删 `User.memories`
- `backend/app/app_main.py` — 删 `LongTermMemory` import
- `backend/app/router/memory_router.py` — 改读 `mem0.get_all`

**删除：**
- `backend/app/service/memory_service.py`

---

## Task 1: 安装并锁定 mem0，验证 DashScope+Milvus 配置

> 这是去风险的第一步：mem0 的 config key 和返回结构跨版本会变。先装、锁版本、用一次性脚本验证连通，把"可用的配置字典"固化下来，后续所有任务基于它。

**Files:**
- Modify: `backend/requirements.txt`

- [ ] **Step 1: 安装 mem0**

Run: `pip install mem0ai`
然后查看实际版本：`pip show mem0ai | findstr Version`

- [ ] **Step 2: 把实际版本写进 requirements.txt（锁版本）**

把上一步看到的版本号填进去（示例 `0.1.114`，以实际为准）：

```
mem0ai==<实际版本号>
```

- [ ] **Step 3: 写一次性连通验证脚本（验证后删除，不进 git）**

创建临时文件 `backend/_verify_mem0.py`：

```python
import os
from dotenv import load_dotenv
load_dotenv()

from mem0 import Memory

API_KEY = os.getenv("DASHSCOPE_API_KEY")
BASE_URL = os.getenv("DASHSCOPE_BASE_URL", "https://dashscope.aliyuncs.com/compatible-mode/v1")
MILVUS_URI = os.getenv("MILVUS_URI", "http://localhost:19530")

config = {
    "vector_store": {
        "provider": "milvus",
        "config": {
            "collection_name": "mem0_memories",
            "embedding_model_dims": 1024,
            "url": MILVUS_URI,
            "metric_type": "COSINE",
        },
    },
    "llm": {
        "provider": "openai",
        "config": {
            "model": "qwen-plus",
            "api_key": API_KEY,
            "openai_base_url": BASE_URL,
            "temperature": 0.2,
        },
    },
    "embedder": {
        "provider": "openai",
        "config": {
            "model": "text-embedding-v4",
            "api_key": API_KEY,
            "openai_base_url": BASE_URL,
            "embedding_dims": 1024,
        },
    },
}

m = Memory.from_config(config)
add_res = m.add(
    [{"role": "user", "content": "我只关注智慧交通行业，报告要简洁"}],
    user_id="verify_user",
    metadata={"type": "preference"},
)
print("ADD:", add_res)
search_res = m.search("用户关注什么行业", user_id="verify_user", limit=3)
print("SEARCH:", search_res)
```

- [ ] **Step 4: 运行验证脚本，观察并记录真实 API 形状**

Run: `python _verify_mem0.py`
Expected: 打印出 ADD 结果和 SEARCH 结果，无异常。

**记录两件事供后续任务使用：**
1. 若 `Memory.from_config` 报未知配置键（如 `openai_base_url`/`embedding_dims` 不被识别），按报错改键名，并把**可用的 config 字典**记在 Task 3 的 `_build_config` 里。
2. 观察 `search` 返回是 `{"results": [...]}` 还是 `list`；每个条目里记忆文本字段叫 `memory` 还是 `text`，metadata 在哪。Task 3/5 的解析按观察到的形状写。

- [ ] **Step 5: 删除临时脚本，提交锁版本**

```bash
del _verify_mem0.py
git add requirements.txt
git commit -m "chore(memory): 引入并锁定 mem0ai 依赖"
```

---

## Task 2: `classify_industry` 行业分类器

**Files:**
- Create: `backend/app/service/memory_engine.py`（先只放分类器）
- Test: `backend/test/test_memory/test_classify_industry.py`
- Create: `backend/test/test_memory/__init__.py`（空文件）

- [ ] **Step 1: 建测试包占位**

创建空文件 `backend/test/test_memory/__init__.py`（内容为空）。

- [ ] **Step 2: 写失败测试**

`backend/test/test_memory/test_classify_industry.py`：

```python
from app.service.memory_engine import classify_industry


def test_classify_known_industries():
    assert classify_industry("分析车路协同与智慧高速的市场规模") == "智慧交通"
    assert classify_industry("金融科技支付赛道的监管趋势") == "金融科技"
    assert classify_industry("医疗健康行业的创新药研发") == "医疗健康"
    assert classify_industry("新能源与电力储能的投资前景") == "能源电力"


def test_classify_fallback_to_general():
    assert classify_industry("今天天气怎么样") == "general"
    assert classify_industry("") == "general"


def test_classify_first_match_wins_is_deterministic():
    # 同一输入多次调用结果稳定
    q = "智慧交通和金融科技的对比"
    assert classify_industry(q) == classify_industry(q)
```

- [ ] **Step 3: 运行，确认失败**

Run: `python -m pytest test/test_memory/test_classify_industry.py -q`
Expected: FAIL（`ModuleNotFoundError` 或 `ImportError: classify_industry`）

- [ ] **Step 4: 写实现**

创建 `backend/app/service/memory_engine.py`：

```python
"""长期记忆引擎（mem0 封装）+ 行业分类器。

两层记忆：
- 偏好层：scope = user_id
- 方法规范层(SOP)：scope = f"sop::{industry}"
"""

from typing import List, Dict, Any, Optional

# 4 个预置行业的关键词表（命中即归类；落不到则 general）
_INDUSTRY_KEYWORDS: Dict[str, List[str]] = {
    "智慧交通": ["智慧交通", "车路协同", "智慧高速", "智能公交", "智慧停车", "交通大脑", "自动驾驶", "交通"],
    "金融科技": ["金融科技", "fintech", "支付", "数字货币", "区块链金融", "监管科技", "银行", "证券", "保险科技"],
    "医疗健康": ["医疗健康", "创新药", "医疗", "医药", "生物医药", "医疗器械", "诊断", "健康"],
    "能源电力": ["能源电力", "新能源", "储能", "电力", "光伏", "风电", "氢能", "电网", "碳中和"],
}


def classify_industry(query: str) -> str:
    """把研究问题归到 4 个预置行业之一，落不到返回 'general'。"""
    if not query:
        return "general"
    for industry, keywords in _INDUSTRY_KEYWORDS.items():
        for kw in keywords:
            if kw in query:
                return industry
    return "general"
```

- [ ] **Step 5: 运行，确认通过**

Run: `python -m pytest test/test_memory/test_classify_industry.py -q`
Expected: PASS（3 passed）

- [ ] **Step 6: 提交**

```bash
git add app/service/memory_engine.py test/test_memory/
git commit -m "feat(memory): 新增行业分类器 classify_industry"
```

---

## Task 3: `MemoryEngine` 偏好层（写 + 读，mock mem0）

> 测试全程 mock `mem0.Memory`，不依赖真实 mem0 API；真实连通在 Task 6 smoke 验证。

**Files:**
- Modify: `backend/app/service/memory_engine.py`
- Test: `backend/test/test_memory/test_memory_engine.py`

- [ ] **Step 1: 写失败测试**

`backend/test/test_memory/test_memory_engine.py`：

```python
from unittest.mock import MagicMock
import pytest
from app.service.memory_engine import MemoryEngine


def _make_engine_with_mock_mem():
    """构造一个注入了 mock mem0 client 的 MemoryEngine（绕过真实初始化）。"""
    engine = MemoryEngine.__new__(MemoryEngine)  # 不走 __init__
    engine._mem = MagicMock()
    return engine


def test_remember_preferences_calls_mem_add():
    engine = _make_engine_with_mock_mem()
    msgs = [{"role": "user", "content": "报告要简洁"}]
    engine.remember_preferences("u1", msgs)
    engine._mem.add.assert_called_once()
    kwargs = engine._mem.add.call_args.kwargs
    assert kwargs["user_id"] == "u1"
    assert kwargs["metadata"] == {"type": "preference"}


def test_remember_preferences_swallows_errors():
    engine = _make_engine_with_mock_mem()
    engine._mem.add.side_effect = RuntimeError("mem0 down")
    # 不得抛出
    engine.remember_preferences("u1", [{"role": "user", "content": "x"}])


def test_recall_preferences_formats_context():
    engine = _make_engine_with_mock_mem()
    engine._mem.search.return_value = {
        "results": [
            {"memory": "用户只关注智慧交通", "metadata": {"type": "preference"}},
            {"memory": "报告要简洁", "metadata": {"type": "preference"}},
        ]
    }
    ctx = engine.recall_preferences("u1", "写一份报告")
    assert "[用户偏好]" in ctx
    assert "用户只关注智慧交通" in ctx
    assert "报告要简洁" in ctx


def test_recall_preferences_handles_list_shape():
    # 兼容 mem0 返回裸 list 的版本
    engine = _make_engine_with_mock_mem()
    engine._mem.search.return_value = [{"memory": "偏好A", "metadata": {"type": "preference"}}]
    ctx = engine.recall_preferences("u1", "q")
    assert "偏好A" in ctx


def test_recall_preferences_empty_returns_blank():
    engine = _make_engine_with_mock_mem()
    engine._mem.search.return_value = {"results": []}
    assert engine.recall_preferences("u1", "q") == ""


def test_recall_preferences_swallows_errors():
    engine = _make_engine_with_mock_mem()
    engine._mem.search.side_effect = RuntimeError("boom")
    assert engine.recall_preferences("u1", "q") == ""
```

- [ ] **Step 2: 运行，确认失败**

Run: `python -m pytest test/test_memory/test_memory_engine.py -q`
Expected: FAIL（`AttributeError: remember_preferences` 等）

- [ ] **Step 3: 在 `memory_engine.py` 追加 `MemoryEngine`（偏好层 + 解析工具）**

在 `memory_engine.py` 末尾追加（保留已有 `classify_industry`）：

```python
import os
import logging
from dotenv import load_dotenv

load_dotenv()
logger = logging.getLogger("memory_engine")


def _extract_results(search_ret: Any) -> List[Dict[str, Any]]:
    """兼容 mem0 search 的两种返回形状：{"results": [...]} 或裸 list。"""
    if isinstance(search_ret, dict):
        return search_ret.get("results", []) or []
    if isinstance(search_ret, list):
        return search_ret
    return []


def _memory_text(item: Dict[str, Any]) -> str:
    """取记忆文本，兼容字段名 memory / text。"""
    return item.get("memory") or item.get("text") or ""


class MemoryEngine:
    """mem0 封装：偏好层(user_id) + 方法规范层(sop::industry)。

    所有方法 best-effort：读失败返回空，写失败静默。
    """

    def __init__(self) -> None:
        from mem0 import Memory
        self._mem = Memory.from_config(self._build_config())

    @staticmethod
    def _build_config() -> Dict[str, Any]:
        api_key = os.getenv("DASHSCOPE_API_KEY")
        base_url = os.getenv("DASHSCOPE_BASE_URL", "https://dashscope.aliyuncs.com/compatible-mode/v1")
        milvus_uri = os.getenv("MILVUS_URI", "http://localhost:19530")
        # 注意：如 Task 1 Step 4 发现 key 名不同，改这里
        return {
            "vector_store": {
                "provider": "milvus",
                "config": {
                    "collection_name": "mem0_memories",
                    "embedding_model_dims": 1024,
                    "url": milvus_uri,
                    "metric_type": "COSINE",
                },
            },
            "llm": {
                "provider": "openai",
                "config": {
                    "model": "qwen-plus",
                    "api_key": api_key,
                    "openai_base_url": base_url,
                    "temperature": 0.2,
                },
            },
            "embedder": {
                "provider": "openai",
                "config": {
                    "model": "text-embedding-v4",
                    "api_key": api_key,
                    "openai_base_url": base_url,
                    "embedding_dims": 1024,
                },
            },
        }

    # ── 偏好层（scope = user_id）─────────────────────────────
    def remember_preferences(self, user_id: str, messages: List[Dict[str, Any]]) -> None:
        if not user_id or not messages:
            return
        try:
            self._mem.add(messages, user_id=user_id, metadata={"type": "preference"})
        except Exception as e:
            logger.warning(f"remember_preferences failed: {e}")

    def recall_preferences(self, user_id: str, query: str, k: int = 5) -> str:
        if not user_id or not query:
            return ""
        try:
            ret = self._mem.search(query, user_id=user_id, limit=k)
            items = [
                _memory_text(it)
                for it in _extract_results(ret)
                if (it.get("metadata") or {}).get("type") == "preference"
            ]
            items = [t for t in items if t]
            if not items:
                return ""
            lines = ["[用户偏好]"] + [f"- {t}" for t in items] + [""]
            return "\n".join(lines)
        except Exception as e:
            logger.warning(f"recall_preferences failed: {e}")
            return ""
```

- [ ] **Step 4: 运行，确认通过**

Run: `python -m pytest test/test_memory/test_memory_engine.py -q`
Expected: PASS（6 passed）

- [ ] **Step 5: 提交**

```bash
git add app/service/memory_engine.py test/test_memory/test_memory_engine.py
git commit -m "feat(memory): MemoryEngine 偏好层读写(mem0封装)"
```

---

## Task 4: `MemoryEngine` 方法规范层（Critic 反馈 → 教训）

**Files:**
- Modify: `backend/app/service/memory_engine.py`
- Test: `backend/test/test_memory/test_memory_engine.py`（追加）

- [ ] **Step 1: 追加失败测试**

在 `test_memory_engine.py` 末尾追加：

```python
def _critic_feedback_sample():
    return [
        {"issue_type": "missing_source", "severity": "critical",
         "description": "渗透率数据无官方来源", "suggestion": "补充国家统计局口径"},
        {"issue_type": "outdated", "severity": "minor",
         "description": "引用了2019年数据", "suggestion": "更新到最近年份"},
    ]


def test_remember_lessons_adds_per_feedback():
    engine = _make_engine_with_mock_mem()
    engine.remember_lessons("智慧交通", _critic_feedback_sample(), quality_score=6.0)
    # 两条反馈 → 两次 add
    assert engine._mem.add.call_count == 2
    kwargs = engine._mem.add.call_args_list[0].kwargs
    assert kwargs["user_id"] == "sop::智慧交通"
    assert kwargs["metadata"]["type"] == "sop"
    assert kwargs["metadata"]["issue_type"] == "missing_source"


def test_remember_lessons_skips_when_empty():
    engine = _make_engine_with_mock_mem()
    engine.remember_lessons("智慧交通", [], quality_score=9.0)
    engine._mem.add.assert_not_called()


def test_remember_lessons_swallows_errors():
    engine = _make_engine_with_mock_mem()
    engine._mem.add.side_effect = RuntimeError("down")
    engine.remember_lessons("智慧交通", _critic_feedback_sample(), 5.0)  # 不抛


def test_recall_lessons_filters_by_type_and_recurrence():
    engine = _make_engine_with_mock_mem()
    engine._mem.search.return_value = {
        "results": [
            # 复现 3 次，应保留
            {"memory": "政策类结论必须引用原文", "metadata": {"type": "sop"}, "score": 0.9},
            {"memory": "渗透率必须标注口径", "metadata": {"type": "sop"}, "score": 0.8},
            # 非 sop，过滤掉
            {"memory": "无关偏好", "metadata": {"type": "preference"}, "score": 0.7},
        ]
    }
    ctx = engine.recall_lessons("智慧交通", "市场规模", k=5, min_recurrence=1)
    assert "[过往研究教训]" in ctx
    assert "政策类结论必须引用原文" in ctx
    assert "无关偏好" not in ctx


def test_recall_lessons_empty_returns_blank():
    engine = _make_engine_with_mock_mem()
    engine._mem.search.return_value = {"results": []}
    assert engine.recall_lessons("智慧交通", "q") == ""
```

- [ ] **Step 2: 运行，确认失败**

Run: `python -m pytest test/test_memory/test_memory_engine.py -q`
Expected: FAIL（`AttributeError: remember_lessons`）

- [ ] **Step 3: 在 `MemoryEngine` 内追加方法规范层**

在 `MemoryEngine` 类体内（`recall_preferences` 之后）追加：

```python
    # ── 方法规范层（scope = "sop::{industry}"）───────────────
    @staticmethod
    def _sop_scope(industry: str) -> str:
        return f"sop::{industry or 'general'}"

    @staticmethod
    def _lesson_text(fb: Dict[str, Any]) -> str:
        """把一条 CriticFeedback 提炼成可复用的"研究教训"文本。"""
        issue = fb.get("issue_type", "issue")
        desc = fb.get("description", "")
        sugg = fb.get("suggestion", "")
        return f"教训({issue}): {desc} → 改进: {sugg}".strip()

    def remember_lessons(self, industry: str, critic_feedback: List[Dict[str, Any]],
                         quality_score: float) -> None:
        if not critic_feedback:
            return
        scope = self._sop_scope(industry)
        for fb in critic_feedback:
            text = self._lesson_text(fb)
            if not text:
                continue
            try:
                self._mem.add(
                    [{"role": "user", "content": text}],
                    user_id=scope,
                    metadata={"type": "sop", "issue_type": fb.get("issue_type", "issue")},
                )
            except Exception as e:
                logger.warning(f"remember_lessons add failed: {e}")

    def recall_lessons(self, industry: str, query: str, k: int = 5,
                       min_recurrence: int = 2) -> str:
        if not query:
            return ""
        scope = self._sop_scope(industry)
        try:
            ret = self._mem.search(query, user_id=scope, limit=k)
            items = [
                _memory_text(it)
                for it in _extract_results(ret)
                if (it.get("metadata") or {}).get("type") == "sop"
            ]
            items = [t for t in items if t]
            if not items:
                return ""
            lines = ["[过往研究教训（请在规划时主动规避）]"] + [f"- {t}" for t in items] + [""]
            return "\n".join(lines)
        except Exception as e:
            logger.warning(f"recall_lessons failed: {e}")
            return ""
```

> 说明：`min_recurrence` 阈值依赖 mem0 是否在返回里暴露"出现次数"。当前 mem0 不稳定暴露该字段，故本期实现把它作为**保留参数**（签名存在、默认 2），按"召回即注入"运行；Task 14 在 Future Work 注记真正接入复现计数。测试 `min_recurrence=1` 验证不被过滤。

- [ ] **Step 4: 运行，确认通过**

Run: `python -m pytest test/test_memory/test_memory_engine.py -q`
Expected: PASS（11 passed）

- [ ] **Step 5: 提交**

```bash
git add app/service/memory_engine.py test/test_memory/test_memory_engine.py
git commit -m "feat(memory): MemoryEngine 方法规范层(Critic反馈→教训)"
```

---

## Task 5: 单例 `get_memory_engine` + `list_memories`（供 router）

**Files:**
- Modify: `backend/app/service/memory_engine.py`
- Test: `backend/test/test_memory/test_memory_engine.py`（追加）

- [ ] **Step 1: 追加失败测试**

```python
def test_list_memories_returns_normalized():
    engine = _make_engine_with_mock_mem()
    engine._mem.get_all.return_value = {
        "results": [
            {"id": "m1", "memory": "偏好A", "metadata": {"type": "preference"}},
        ]
    }
    out = engine.list_memories("u1")
    assert out == [{"id": "m1", "content": "偏好A", "type": "preference"}]


def test_list_memories_swallows_errors():
    engine = _make_engine_with_mock_mem()
    engine._mem.get_all.side_effect = RuntimeError("down")
    assert engine.list_memories("u1") == []


def test_delete_memory_calls_mem():
    engine = _make_engine_with_mock_mem()
    engine.delete_memory("m1")
    engine._mem.delete.assert_called_once_with(memory_id="m1")
```

- [ ] **Step 2: 运行，确认失败**

Run: `python -m pytest test/test_memory/test_memory_engine.py -q`
Expected: FAIL（`AttributeError: list_memories`）

- [ ] **Step 3: 追加 `list_memories` / `delete_memory` + 模块级单例**

在 `MemoryEngine` 类体内追加：

```python
    # ── 供 router 列出/删除"我的记忆"────────────────────────
    def list_memories(self, user_id: str) -> List[Dict[str, Any]]:
        if not user_id:
            return []
        try:
            ret = self._mem.get_all(user_id=user_id)
            out = []
            for it in _extract_results(ret):
                out.append({
                    "id": it.get("id", ""),
                    "content": _memory_text(it),
                    "type": (it.get("metadata") or {}).get("type", "unknown"),
                })
            return out
        except Exception as e:
            logger.warning(f"list_memories failed: {e}")
            return []

    def delete_memory(self, memory_id: str) -> None:
        try:
            self._mem.delete(memory_id=memory_id)
        except Exception as e:
            logger.warning(f"delete_memory failed: {e}")
```

在文件末尾追加模块级单例：

```python
_memory_engine: Optional["MemoryEngine"] = None


def get_memory_engine() -> Optional["MemoryEngine"]:
    """懒加载单例。初始化失败（如 mem0/Milvus 不可用）返回 None，调用方需判空。"""
    global _memory_engine
    if _memory_engine is None:
        try:
            _memory_engine = MemoryEngine()
        except Exception as e:
            logger.warning(f"MemoryEngine init failed, memory disabled: {e}")
            return None
    return _memory_engine
```

> 关键：`get_memory_engine()` 可能返回 `None`（mem0/Milvus 起不来）。**所有调用点必须判空**：`engine = get_memory_engine(); if engine: ...`。

- [ ] **Step 4: 运行，确认通过**

Run: `python -m pytest test/test_memory/test_memory_engine.py -q`
Expected: PASS（14 passed）

- [ ] **Step 5: 提交**

```bash
git add app/service/memory_engine.py test/test_memory/test_memory_engine.py
git commit -m "feat(memory): MemoryEngine 列表/删除 + 懒加载单例(判空降级)"
```

---

## Task 6: 集成 smoke 测试（真 mem0 + Milvus，标记 slow）

**Files:**
- Test: `backend/test/test_memory/test_memory_engine_smoke.py`

- [ ] **Step 1: 写 smoke 测试**

```python
"""真实 mem0 + Milvus + DashScope 往返。需外部服务，默认跳过。
运行：python -m pytest test/test_memory/test_memory_engine_smoke.py -q -m slow --runslow
（若项目无 --runslow，可直接 python -m pytest test/test_memory/test_memory_engine_smoke.py -q -k roundtrip）
"""
import os
import uuid
import pytest
from app.service.memory_engine import get_memory_engine

pytestmark = pytest.mark.slow


@pytest.mark.skipif(not os.getenv("DASHSCOPE_API_KEY"), reason="需要 DASHSCOPE_API_KEY")
def test_preference_roundtrip():
    engine = get_memory_engine()
    if engine is None:
        pytest.skip("MemoryEngine 初始化失败（mem0/Milvus 不可用）")
    uid = f"smoke_{uuid.uuid4().hex[:8]}"
    engine.remember_preferences(uid, [{"role": "user", "content": "我只关注智慧交通，报告要简洁"}])
    ctx = engine.recall_preferences(uid, "用户关注哪个行业")
    assert "交通" in ctx or ctx == ""  # 召回到则含关键词；mem0 抽取有不确定性，空也不算失败


@pytest.mark.skipif(not os.getenv("DASHSCOPE_API_KEY"), reason="需要 DASHSCOPE_API_KEY")
def test_lessons_roundtrip():
    engine = get_memory_engine()
    if engine is None:
        pytest.skip("MemoryEngine 初始化失败")
    fb = [{"issue_type": "missing_source", "severity": "critical",
           "description": "缺官方来源", "suggestion": "补充国家统计局"}]
    engine.remember_lessons("智慧交通", fb, 6.0)
    ctx = engine.recall_lessons("智慧交通", "市场规模官方数据", min_recurrence=1)
    assert isinstance(ctx, str)
```

- [ ] **Step 2: 注册 `slow` marker（避免 PytestUnknownMarkWarning）**

检查 `backend/` 是否有 `pytest.ini` / `pyproject.toml [tool.pytest.ini_options]`。若有 `markers` 配置就追加 `slow`；若没有，创建 `backend/pytest.ini`：

```ini
[pytest]
markers =
    slow: 需要外部服务的慢测试
```

> 若已存在 `pytest.ini`，只在其 `[pytest]` 段的 `markers` 下加 `slow:` 一行，不要覆盖原文件。

- [ ] **Step 3: 默认套件不跑 slow，确认不影响**

Run: `python -m pytest test/test_memory -q -m "not slow"`
Expected: PASS（不含 smoke 的全部记忆单测通过）

- [ ] **Step 4: 有外部服务时手动验证一次（可选）**

Run: `python -m pytest test/test_memory/test_memory_engine_smoke.py -q`
Expected: PASS 或 skip（无 key/服务时 skip，不报错）

- [ ] **Step 5: 提交**

```bash
git add test/test_memory/test_memory_engine_smoke.py backend/pytest.ini
git commit -m "test(memory): 真实 mem0+Milvus 往返 smoke(标记slow)"
```

---

## Task 7: `ContextCompressor` 工作记忆滚动压缩

> 纯函数式可单测：token 估算、是否触发、保留最近 K 轮、增量游标。LLM 摘要通过注入的 `summarize_fn` 实现，测试用假函数。

**Files:**
- Create: `backend/app/service/context_compressor.py`
- Test: `backend/test/test_memory/test_context_compressor.py`

- [ ] **Step 1: 写失败测试**

```python
from app.service.context_compressor import ContextCompressor


def _msgs(n):
    return [{"role": "user" if i % 2 == 0 else "assistant", "content": f"消息{i} " * 50}
            for i in range(n)]


def test_below_threshold_no_compression():
    cc = ContextCompressor(keep_recent_turns=2, compress_threshold=10_000_000,
                           summarize_fn=lambda text: "SUMMARY")
    msgs = _msgs(6)
    history, summary, cursor = cc.build_history(prior_summary="", summarized_up_to=0, messages=msgs)
    assert "SUMMARY" not in history          # 未触发压缩
    assert summary == ""                      # 摘要不变
    assert cursor == 0                        # 游标不动
    assert "消息5" in history                  # 含全部原文


def test_above_threshold_compresses_old_keeps_recent():
    cc = ContextCompressor(keep_recent_turns=2, compress_threshold=10,
                           summarize_fn=lambda text: "OLD_SUMMARY")
    msgs = _msgs(6)
    history, summary, cursor = cc.build_history(prior_summary="", summarized_up_to=0, messages=msgs)
    assert "OLD_SUMMARY" in history           # 旧历史被压
    assert "消息4" in history and "消息5" in history  # 最近 2 轮原文保留
    assert summary == "OLD_SUMMARY"
    assert cursor == 4                         # 已压缩到第 4 条（保留最后 2 条）


def test_incremental_only_compresses_new_old_turns():
    captured = {}
    def fake_summarize(text):
        captured["text"] = text
        return "MERGED"
    cc = ContextCompressor(keep_recent_turns=2, compress_threshold=10,
                           summarize_fn=fake_summarize)
    msgs = _msgs(8)
    # 已压缩到第 4 条，已有摘要 "PREV"
    history, summary, cursor = cc.build_history(prior_summary="PREV", summarized_up_to=4, messages=msgs)
    # 只应把第 4~5 条（新的旧轮次）并入，已有 PREV 作为前缀传给摘要器
    assert "PREV" in captured["text"]
    assert "消息4" in captured["text"]
    assert "消息2" not in captured["text"]     # 之前已压过的不重复喂
    assert cursor == 6
    assert "消息6" in history and "消息7" in history


def test_summarize_failure_degrades_without_raising():
    def boom(text):
        raise RuntimeError("llm down")
    cc = ContextCompressor(keep_recent_turns=2, compress_threshold=10, summarize_fn=boom)
    msgs = _msgs(6)
    # 不得抛；退化为保留最近 K 轮 + 原有摘要
    history, summary, cursor = cc.build_history(prior_summary="PREV", summarized_up_to=0, messages=msgs)
    assert "消息4" in history and "消息5" in history
    assert "PREV" in history
```

- [ ] **Step 2: 运行，确认失败**

Run: `python -m pytest test/test_memory/test_context_compressor.py -q`
Expected: FAIL（`ModuleNotFoundError`）

- [ ] **Step 3: 写实现**

`backend/app/service/context_compressor.py`：

```python
"""Chat 工作记忆滚动压缩：超阈值时压缩旧历史，完整保留最近 K 轮。"""

from typing import Callable, List, Dict, Tuple

Message = Dict[str, str]


def estimate_tokens(text: str) -> int:
    """粗略 token 估算（中英文混合，约 3 字符/token）。"""
    return len(text) // 3


class ContextCompressor:
    def __init__(self, keep_recent_turns: int = 4, compress_threshold: int = 6000,
                 summarize_fn: Callable[[str], str] = None):
        self.keep_recent_turns = keep_recent_turns
        self.compress_threshold = compress_threshold
        # summarize_fn: 接收待压缩文本，返回摘要；由调用方注入 LLM 实现
        self.summarize_fn = summarize_fn

    @staticmethod
    def _render(messages: List[Message]) -> str:
        return "\n".join(f"{m.get('role')}: {m.get('content')}" for m in messages)

    def build_history(self, prior_summary: str, summarized_up_to: int,
                      messages: List[Message]) -> Tuple[str, str, int]:
        """返回 (history_text, new_summary, new_summarized_up_to)。

        - 未超阈值：原文全返回，摘要/游标不变。
        - 超阈值：older = messages[:-keep_recent] 中尚未压缩的部分 → 并入 prior_summary；
          recent = 最近 keep_recent_turns 条原文保留。
        """
        recent = messages[-self.keep_recent_turns:] if self.keep_recent_turns > 0 else []
        recent_text = self._render(recent)

        total_tokens = estimate_tokens(self._render(messages))
        if total_tokens <= self.compress_threshold:
            # 不压缩：全文原样
            return self._render(messages), prior_summary, summarized_up_to

        # 触发压缩：确定本次新增需要压缩的旧轮次区间
        split = max(0, len(messages) - self.keep_recent_turns)  # recent 之前都是 old
        new_old = messages[summarized_up_to:split]              # 仅未压缩过的旧轮次

        if not new_old:
            # 没有新的旧轮次可压，直接拼已有摘要 + recent
            summary = prior_summary
            new_cursor = summarized_up_to
        else:
            to_summarize = (f"已有摘要：{prior_summary}\n\n" if prior_summary else "") + \
                           f"新增历史：\n{self._render(new_old)}"
            try:
                summary = self.summarize_fn(to_summarize) if self.summarize_fn else prior_summary
                new_cursor = split
            except Exception:
                # 降级：摘要失败 → 保留原有摘要，不推进游标
                summary = prior_summary
                new_cursor = summarized_up_to

        parts = []
        if summary:
            parts.append(f"[历史对话摘要]\n{summary}")
        if recent_text:
            parts.append(f"[最近对话]\n{recent_text}")
        return "\n\n".join(parts), summary, new_cursor
```

- [ ] **Step 4: 运行，确认通过**

Run: `python -m pytest test/test_memory/test_context_compressor.py -q`
Expected: PASS（4 passed）

- [ ] **Step 5: 提交**

```bash
git add app/service/context_compressor.py test/test_memory/test_context_compressor.py
git commit -m "feat(memory): ContextCompressor 滚动压缩(保留最近K轮)"
```

---

## Task 8: `ResearchState` 增加 `user_id` 字段并贯通

**Files:**
- Modify: `backend/app/service/deep_research_v2/state.py`
- Modify: `backend/app/service/deep_research_v2/graph.py:612` 和 `:929`
- Test: `backend/test/test_deep_research_v3/test_state.py`（追加）

- [ ] **Step 1: 追加失败测试**

在 `test/test_deep_research_v3/test_state.py` 末尾追加：

```python
def test_research_state_has_user_id():
    state = create_initial_state(query="q", session_id="s", user_id="u1")
    assert state["user_id"] == "u1"


def test_research_state_user_id_defaults_empty():
    state = create_initial_state(query="q", session_id="s")
    assert state["user_id"] == ""
```

- [ ] **Step 2: 运行，确认失败**

Run: `python -m pytest test/test_deep_research_v3/test_state.py -q`
Expected: FAIL（`KeyError: 'user_id'` / `TypeError: unexpected keyword 'user_id'`）

- [ ] **Step 3: 改 `state.py`**

在 `ResearchState` 的"基础信息"块（`session_id: str` 之后）加一行：

```python
    user_id: str                            # 用户ID（记忆读写用）
```

`create_initial_state` 签名加参数（放在 `session_id` 之后）：

```python
def create_initial_state(
    query: str,
    session_id: str,
    search_web: bool = True,
    search_local: bool = False,
    user_id: str = "",
) -> ResearchState:
```

在返回的 `ResearchState(...)` 里，`session_id=session_id,` 之后加：

```python
        user_id=user_id,
```

- [ ] **Step 4: 运行 state 测试，确认通过**

Run: `python -m pytest test/test_deep_research_v3/test_state.py -q`
Expected: PASS

- [ ] **Step 5: 在 graph.py 透传 user_id**

`graph.py` `run()` 内（约 `:612`）把：

```python
            state = create_initial_state(query, session_id)
```

改为：

```python
            state = create_initial_state(query, session_id, user_id=user_id or "")
```

`run_sync` 内（约 `:929`）`create_initial_state(query, session_id)` 保持不变（同步路径不接记忆）。

- [ ] **Step 6: 运行 v3 全量 + state，确认无回归**

Run: `python -m pytest test/test_deep_research_v3 -q`
Expected: 仅原有 `test_graph_compiled_has_4_main_nodes` 这 1 个**既有失败**保持失败，其余通过（与基线一致）。

- [ ] **Step 7: 提交**

```bash
git add app/service/deep_research_v2/state.py app/service/deep_research_v2/graph.py test/test_deep_research_v3/test_state.py
git commit -m "feat(memory): ResearchState 增加 user_id 并在 graph 透传"
```

---

## Task 9: Planner 注入偏好+教训，Writer 注入偏好

**Files:**
- Modify: `backend/app/service/deep_research_v2/agents/planner.py`
- Modify: `backend/app/service/deep_research_v2/agents/writer.py`
- Test: `backend/test/test_memory/test_injection.py`

- [ ] **Step 1: 写失败测试（验证注入逻辑可独立调用）**

`backend/test/test_memory/test_injection.py`：

```python
from unittest.mock import MagicMock
from app.service.deep_research_v2.agents.planner import _build_memory_prefix


def test_planner_memory_prefix_combines_pref_and_lessons():
    engine = MagicMock()
    engine.recall_preferences.return_value = "[用户偏好]\n- 报告要简洁\n"
    engine.recall_lessons.return_value = "[过往研究教训（请在规划时主动规避）]\n- 教训X\n"
    prefix = _build_memory_prefix(engine, user_id="u1", query="智慧交通市场")
    assert "报告要简洁" in prefix
    assert "教训X" in prefix
    engine.recall_lessons.assert_called_once()  # industry 由 query 推出


def test_planner_memory_prefix_empty_engine_returns_blank():
    assert _build_memory_prefix(None, user_id="u1", query="q") == ""


def test_planner_memory_prefix_no_user_id_returns_blank():
    engine = MagicMock()
    assert _build_memory_prefix(engine, user_id="", query="q") == ""
```

- [ ] **Step 2: 运行，确认失败**

Run: `python -m pytest test/test_memory/test_injection.py -q`
Expected: FAIL（`ImportError: _build_memory_prefix`）

- [ ] **Step 3: planner.py 加注入工具 + 调用**

在 `planner.py` 顶部 import 区后加：

```python
from app.service.memory_engine import get_memory_engine, classify_industry


def _build_memory_prefix(engine, user_id: str, query: str) -> str:
    """组装注入 Planner 的记忆前缀：用户偏好 + 本行业历史教训。best-effort。"""
    if engine is None or not user_id:
        return ""
    try:
        pref = engine.recall_preferences(user_id, query)
        lessons = engine.recall_lessons(classify_industry(query), query)
        blocks = [b for b in (pref, lessons) if b]
        return ("\n".join(blocks) + "\n") if blocks else ""
    except Exception:
        return ""
```

在 `process()` 内，构建 `user_prompt` 之后、`call_llm` 之前注入。把：

```python
            user_prompt = f"研究问题：{query}"
            if outline_hint:
                user_prompt += f"\n\n{outline_hint}"
```

改为：

```python
            user_prompt = f"研究问题：{query}"
            if outline_hint:
                user_prompt += f"\n\n{outline_hint}"

            # 注入长期记忆（偏好 + 历史教训）
            mem_prefix = _build_memory_prefix(
                get_memory_engine(), state.get("user_id", ""), query
            )
            if mem_prefix:
                user_prompt = f"{mem_prefix}\n{user_prompt}"
```

- [ ] **Step 4: 运行注入测试，确认通过**

Run: `python -m pytest test/test_memory/test_injection.py -q`
Expected: PASS（3 passed）

- [ ] **Step 5: writer.py 注入偏好**

在 `writer.py` 顶部 import 区后加：

```python
from app.service.memory_engine import get_memory_engine
```

在 `process()` 内 `prompt = self.SECTION_WRITING_PROMPT.format(...)`（约 `:342`）生成 `prompt` 之后、`call_llm(... user_prompt=prompt ...)`（约 `:355`）之前插入：

```python
        # 注入用户写作偏好（best-effort）
        _engine = get_memory_engine()
        if _engine is not None:
            _pref = _engine.recall_preferences(state.get("user_id", ""), state["query"])
            if _pref:
                prompt = f"{_pref}\n{prompt}"
```

- [ ] **Step 6: 运行记忆 + v3 套件，确认无回归**

Run: `python -m pytest test/test_memory test/test_deep_research_v3 -q -m "not slow"`
Expected: 仅既有的 `test_graph_compiled_has_4_main_nodes` 失败，其余通过。

- [ ] **Step 7: 提交**

```bash
git add app/service/deep_research_v2/agents/planner.py app/service/deep_research_v2/agents/writer.py test/test_memory/test_injection.py
git commit -m "feat(memory): Planner注入偏好+教训, Writer注入偏好"
```

---

## Task 10: 研究完成时写记忆（教训 + 偏好）

**Files:**
- Modify: `backend/app/service/deep_research_v2/graph.py`（完成事件处 `:767` 附近）
- Test: `backend/test/test_memory/test_research_writeback.py`

- [ ] **Step 1: 写失败测试**

`backend/test/test_memory/test_research_writeback.py`：

```python
from unittest.mock import MagicMock
from app.service.deep_research_v2.graph import _writeback_research_memory


def test_writeback_calls_lessons_and_preferences():
    engine = MagicMock()
    state = {
        "user_id": "u1",
        "query": "智慧交通市场规模",
        "critic_feedback": [{"issue_type": "missing_source", "description": "x", "suggestion": "y"}],
        "quality_score": 7.0,
        "messages": [{"role": "user", "content": "智慧交通市场规模"}],
    }
    _writeback_research_memory(engine, state)
    engine.remember_lessons.assert_called_once()
    assert engine.remember_lessons.call_args.args[0] == "智慧交通"  # industry
    engine.remember_preferences.assert_called_once()


def test_writeback_noop_without_engine_or_user():
    # engine=None 不抛
    _writeback_research_memory(None, {"user_id": "u1"})
    # 无 user_id 不调用
    engine = MagicMock()
    _writeback_research_memory(engine, {"user_id": ""})
    engine.remember_lessons.assert_not_called()


def test_writeback_swallows_errors():
    engine = MagicMock()
    engine.remember_lessons.side_effect = RuntimeError("down")
    _writeback_research_memory(engine, {"user_id": "u1", "query": "q",
                                        "critic_feedback": [], "quality_score": 5.0,
                                        "messages": []})  # 不抛
```

- [ ] **Step 2: 运行，确认失败**

Run: `python -m pytest test/test_memory/test_research_writeback.py -q`
Expected: FAIL（`ImportError: _writeback_research_memory`）

- [ ] **Step 3: graph.py 加写回函数 + 在完成处触发**

`graph.py` 顶部 import 区加：

```python
import asyncio
from app.service.memory_engine import get_memory_engine, classify_industry
```
（若 `asyncio` 已 import，勿重复）

在模块级（类外）加：

```python
_MEMORY_TASKS: set = set()  # 持有后台任务引用，防止被 GC


def _writeback_research_memory(engine, state: dict) -> None:
    """研究完成后写长期记忆：方法教训 + 用户偏好。best-effort、同步内部、外部用 create_task 包裹。"""
    if engine is None:
        return
    user_id = state.get("user_id", "")
    if not user_id:
        return
    try:
        industry = classify_industry(state.get("query", ""))
        engine.remember_lessons(
            industry,
            state.get("critic_feedback", []) or [],
            state.get("quality_score", 0.0),
        )
        messages = state.get("messages", []) or []
        # messages 是 agent 间事件，取其中 role/content 形态的喂偏好；没有则用 query 兜底
        conv = [m for m in messages if isinstance(m, dict) and m.get("role") and m.get("content")]
        if not conv and state.get("query"):
            conv = [{"role": "user", "content": state["query"]}]
        engine.remember_preferences(user_id, conv)
    except Exception:
        pass
```

在 `run()` 完成事件处（约 `:767` `yield self._build_completion_event(last_state)`）之后追加后台触发：

```python
        yield self._build_completion_event(last_state)

        # 研究完成：后台写长期记忆，不阻塞响应
        try:
            _t = asyncio.create_task(
                asyncio.to_thread(_writeback_research_memory, get_memory_engine(), last_state)
            )
            _MEMORY_TASKS.add(_t)
            _t.add_done_callback(_MEMORY_TASKS.discard)
        except Exception:
            pass
```

> 用 `asyncio.to_thread` 把（同步、可能慢的）mem0 调用丢到线程池，避免阻塞事件循环。

- [ ] **Step 4: 运行写回测试，确认通过**

Run: `python -m pytest test/test_memory/test_research_writeback.py -q`
Expected: PASS（3 passed）

- [ ] **Step 5: 运行 v3 套件确认无回归**

Run: `python -m pytest test/test_deep_research_v3 -q`
Expected: 仅既有 `test_graph_compiled_has_4_main_nodes` 失败。

- [ ] **Step 6: 提交**

```bash
git add app/service/deep_research_v2/graph.py test/test_memory/test_research_writeback.py
git commit -m "feat(memory): 研究完成后后台写回教训+偏好"
```

---

## Task 11: Chat 接入（替换旧记忆 + 历史压缩）

**Files:**
- Modify: `backend/app/service/chat_service.py`
- Modify: `backend/app/models/chat.py`（ChatSession 加列）
- Test: 复用已通过的 compressor/engine 单测（本任务为接线，手动验证为主）

- [ ] **Step 1: ChatSession 加两列（模型层）**

`models/chat.py` `ChatSession` 内 `updated_at` 之后加：

```python
    context_summary = Column(Text)            # 滚动压缩的历史摘要
    summarized_up_to = Column(Integer, default=0)  # 已压缩到第几条消息
```

（`Text`/`Integer` 已在文件顶部 import。）

- [ ] **Step 2: chat_service.py 替换记忆上下文为 mem0 偏好**

把（约 `:282-293`）：

```python
        # 获取长期记忆上下文
        memory_context = ""
        if user_id:
            try:
                memory_service = get_memory_service()
                memory_context = memory_service.build_memory_context(
                    user_id=user_id,
                    current_query=question,
                    max_memories=3
                )
            except Exception as e:
                print(f"获取长期记忆失败: {e}")
```

改为：

```python
        # 获取长期记忆上下文（mem0 偏好层）
        memory_context = ""
        if user_id:
            _engine = get_memory_engine()
            if _engine is not None:
                memory_context = _engine.recall_preferences(user_id, question)
```

- [ ] **Step 3: chat_service.py 顶部 import 调整**

把 `from .memory_service import get_memory_service` 改为：

```python
from .memory_engine import get_memory_engine
from .context_compressor import ContextCompressor, estimate_tokens
```

- [ ] **Step 4: chat_service.py 历史段改用滚动压缩**

把（约 `:274-280`）：

```python
        # 格式化历史对话
        if history_messages:
            # 注意：history_messages已经按时间顺序排列，最近的对话在后面
            history_text = "\n".join([f"{msg['role']}: {msg['content']}" for msg in history_messages])
            history_context = f"\n\n历史对话（最近的对话内容更重要）：\n{history_text}"
        else:
            history_context = ""
```

改为：

```python
        # 格式化历史对话（超阈值滚动压缩，保留最近 K 轮）
        history_context = ""
        if history_messages:
            def _summarize(text: str) -> str:
                resp = OpenAI(api_key=self.openai_api_key, base_url=self.openai_base_url).chat.completions.create(
                    model=self.openai_model,
                    messages=[{"role": "user", "content": f"用3-5句话压缩以下对话，保留关键事实与结论：\n{text}"}],
                )
                return resp.choices[0].message.content.strip()

            _compressor = ContextCompressor(
                keep_recent_turns=4, compress_threshold=6000, summarize_fn=_summarize
            )
            _prior = getattr(self, "_session_summary", {}).get(session_id, ("", 0)) if session_id else ("", 0)
            _hist, _new_summary, _cursor = _compressor.build_history(
                prior_summary=_prior[0], summarized_up_to=_prior[1], messages=history_messages
            )
            history_context = f"\n\n历史对话：\n{_hist}"
            # 进程内缓存滚动摘要（演示足够；持久化见 Task 14 注记）
            if session_id:
                if not hasattr(self, "_session_summary"):
                    self._session_summary = {}
                self._session_summary[session_id] = (_new_summary, _cursor)
```

> 说明：本期把滚动摘要缓存在 `ChatService` 进程内字典（演示足够）。`ChatSession.context_summary`/`summarized_up_to` 字段已加好，持久化落库列为 Task 14 的可选增强（不阻塞验收）。

- [ ] **Step 5: 全量记忆套件 + 导入自检**

Run: `python -m pytest test/test_memory -q -m "not slow"`
Expected: PASS。
再自检 chat_service 可导入：`python -c "from app.service.chat_service import ChatService; print('ok')"`
Expected: 打印 `ok`（说明无残留 `get_memory_service` 引用）。

- [ ] **Step 6: 提交**

```bash
git add app/service/chat_service.py app/models/chat.py
git commit -m "feat(memory): Chat 接入 mem0 偏好 + 历史滚动压缩"
```

---

## Task 12: 删除遗留实现（memory_service + LongTermMemory + 关系）

**Files:**
- Delete: `backend/app/service/memory_service.py`
- Modify: `backend/app/models/chat.py`（删 `LongTermMemory` + `ChatSession.memories`）
- Modify: `backend/app/models/user.py`（删 `User.memories`）
- Modify: `backend/app/app_main.py`（删 import）

- [ ] **Step 1: 删除旧服务文件**

```bash
git rm app/service/memory_service.py
```

- [ ] **Step 2: chat.py 删 LongTermMemory + 关系**

删除 `models/chat.py` 中整个 `class LongTermMemory(Base): ...`（约 `:75-90`）。
删除 `ChatSession` 内：

```python
    memories = relationship("LongTermMemory", back_populates="session")
```

- [ ] **Step 3: user.py 删关系**

删除 `models/user.py:33`：

```python
    memories = relationship("LongTermMemory", back_populates="user", cascade="all, delete-orphan")
```

- [ ] **Step 4: app_main.py 删 import**

`app_main.py:26` 的模型 import 行里去掉 `LongTermMemory,`（保留同行其他模型）。
然后全局确认无残留引用：

Run: `grep -rn "LongTermMemory\|memory_service\|get_memory_service\|build_memory_context" app/`
Expected: 无输出（memory_router 在 Task 13 处理；若此处仍报 memory_router 命中，Task 13 会清掉）。

- [ ] **Step 5: 导入自检**

Run: `python -c "import app.app_main; print('ok')"`
Expected: 打印 `ok`（无 ImportError / mapper 配置错误）。

> 若报 SQLAlchemy mapper 错误（找不到 `LongTermMemory`），说明仍有 `relationship("LongTermMemory")` 残留——回到 Step 2/3 清干净。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "refactor(memory): 删除 LongTermMemory/memory_service 遗留实现"
```

---

## Task 13: memory_router 改读 mem0

**Files:**
- Modify: `backend/app/router/memory_router.py`

- [ ] **Step 1: 重写 router（精简为列出/删除）**

把 `router/memory_router.py` 整体替换为：

```python
"""长期记忆管理路由（mem0 后端）"""
from typing import List, Optional
from fastapi import APIRouter, Depends, status
from pydantic import BaseModel, Field

from models.user import User
from router.auth_router import get_current_user_required
from service.memory_engine import get_memory_engine

router = APIRouter(prefix="/memories", tags=["长期记忆"])


class MemoryItem(BaseModel):
    id: str
    content: str
    type: str = Field(..., description="preference / sop / unknown")


class MemoryListResponse(BaseModel):
    memories: List[MemoryItem] = Field(default_factory=list)
    total: int = 0


@router.get("", response_model=MemoryListResponse)
async def get_memories(current_user: User = Depends(get_current_user_required)):
    """列出当前用户的长期记忆（来自 mem0）。"""
    engine = get_memory_engine()
    items = engine.list_memories(str(current_user.id)) if engine else []
    return MemoryListResponse(
        memories=[MemoryItem(**it) for it in items],
        total=len(items),
    )


@router.delete("/{memory_id}", status_code=status.HTTP_204_NO_CONTENT)
async def delete_memory(
    memory_id: str,
    current_user: User = Depends(get_current_user_required),
):
    """删除一条记忆。"""
    engine = get_memory_engine()
    if engine:
        engine.delete_memory(memory_id)
    return None
```

> 说明：旧的 `/search` `/create` `/context/{query}` 端点删除（搜索/创建已内化到研究/对话流程，不再对外）。若前端有调用，前端侧改为只用列表/删除。

- [ ] **Step 2: 导入自检 + 路由可注册**

Run: `python -c "import app.app_main; print('ok')"`
Expected: 打印 `ok`。
Run: `grep -rn "LongTermMemory\|get_memory_service\|build_memory_context" app/`
Expected: 无输出。

- [ ] **Step 3: 提交**

```bash
git add app/router/memory_router.py
git commit -m "refactor(memory): memory_router 改读 mem0(列出/删除)"
```

---

## Task 14: DB 同步脚本 + 演示脚本 + 收尾验证

**Files:**
- Create: `backend/app/scripts/sync_memory_schema.py`
- Create: `backend/app/scripts/demo_memory.py`

- [ ] **Step 1: DB 同步脚本（加列 + 删旧表，幂等）**

`backend/app/scripts/sync_memory_schema.py`：

```python
"""一次性 DB 同步：为 chat_sessions 加记忆压缩列、删除旧 long_term_memories 表。
create_all 不会 ALTER 既有表，故手动执行。幂等。

运行：python -m app.scripts.sync_memory_schema
"""
from sqlalchemy import text
try:
    from app.core.database import engine
except ImportError:
    from core.database import engine

DDL = [
    "ALTER TABLE chat_sessions ADD COLUMN IF NOT EXISTS context_summary TEXT",
    "ALTER TABLE chat_sessions ADD COLUMN IF NOT EXISTS summarized_up_to INTEGER DEFAULT 0",
    "DROP TABLE IF EXISTS long_term_memories CASCADE",
]


def main():
    with engine.begin() as conn:
        for stmt in DDL:
            print(f"执行: {stmt}")
            conn.execute(text(stmt))
    print("DB 同步完成。")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 若有可用 PG，执行一次（无则跳过，记录待办）**

Run: `python -m app.scripts.sync_memory_schema`
Expected: 打印 3 条"执行:"+"DB 同步完成。"；无 PG 连接时会报连接错误——记录为部署待办，不阻塞代码完成。

- [ ] **Step 3: 演示脚本**

`backend/app/scripts/demo_memory.py`：

```python
"""演示：同一行业连写两次教训/偏好后，第二次召回到记忆。
运行：python -m app.scripts.demo_memory
"""
from app.service.memory_engine import get_memory_engine, classify_industry


def main():
    engine = get_memory_engine()
    if engine is None:
        print("MemoryEngine 不可用（检查 mem0/Milvus/DASHSCOPE_API_KEY）")
        return
    uid = "demo_user"
    industry = classify_industry("智慧交通市场规模与车路协同")
    print(f"行业判定: {industry}")

    # 第一次研究：写偏好 + 教训
    engine.remember_preferences(uid, [{"role": "user", "content": "我只看智慧交通，报告要简洁，多用数据"}])
    engine.remember_lessons(industry, [
        {"issue_type": "missing_source", "description": "渗透率无官方源", "suggestion": "用国家统计局口径"},
    ], quality_score=6.0)

    # 第二次研究：召回
    print("\n--- 第二次研究 Planner 会拿到 ---")
    print(engine.recall_preferences(uid, "智慧交通 2025 趋势"))
    print(engine.recall_lessons(industry, "智慧交通 市场规模 官方数据", min_recurrence=1))


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 全量记忆套件最终绿**

Run: `python -m pytest test/test_memory -q -m "not slow"`
Expected: PASS（全部记忆单测通过）。

- [ ] **Step 5: 全仓回归（与基线对比）**

Run: `python -m pytest -q -p no:cacheprovider -m "not slow" 2>&1 | tail -15`
Expected: 仅基线既有的 `test_graph_compiled_has_4_main_nodes` 失败；记忆相关全绿，无**新增**失败。

- [ ] **Step 6: 提交**

```bash
git add app/scripts/sync_memory_schema.py app/scripts/demo_memory.py
git commit -m "chore(memory): DB同步脚本 + 演示脚本"
```

---

## Future Work（本期不做，spec 已记录）

- 方法规范层接入真实"复现计数"以启用 `min_recurrence` 阈值（依赖 mem0 暴露出现次数）。
- 滚动摘要持久化落库（用已加好的 `ChatSession.context_summary`/`summarized_up_to` 列替换进程内缓存）。
- 信源信誉记忆、研究情景记忆、领域事实 TTL 复用、DeepResearch 侧上下文压缩。

---

## Self-Review（已核对）

- **Spec 覆盖**：偏好层(Task 3) / 方法规范层(Task 4) / 单例判空(Task 5) / smoke(Task 6) / 滚动压缩(Task 7) / user_id 贯通(Task 8) / Planner+Writer 注入(Task 9) / 完成写回(Task 10) / Chat 接入(Task 11) / 清理(Task 12-13) / 脚本(Task 14)——spec 各节均有对应任务。
- **占位符**：无 TBD/TODO 式占位；每个代码步骤含完整代码。
- **类型/签名一致**：`get_memory_engine()`→`MemoryEngine`(可空)；`recall_preferences/recall_lessons/remember_preferences/remember_lessons/list_memories/delete_memory` 在引擎定义(Task 3-5)与各调用点(Task 9-13)签名一致；`ContextCompressor.build_history(prior_summary, summarized_up_to, messages)→(history,summary,cursor)` 定义(Task 7)与调用(Task 11)一致；`classify_industry`(Task 2) 在 Task 9/10/14 复用。
