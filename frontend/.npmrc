# 强制使用 legacy peer-deps 行为（npm 6 的语义）。
#
# 原因：项目使用 React 19，但部分依赖（如 ahooks）的 peer dep 声明还停在
# React 18，npm 7+ 默认严格校验会直接报错。这些库在 React 19 下实际可运行，
# 等上游修正 peer dep 范围后可以移除此开关。
#
# 影响面：仅这个目录内执行的 npm 命令；不影响全局或父目录的 npm 行为。
legacy-peer-deps=true
