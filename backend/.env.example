# ===== App =====
APP_NAME=industry-research-assistant
ENV=dev
DEBUG=true
LOG_LEVEL=INFO

# ===== Server =====
HOST=0.0.0.0
PORT=8000

# NOTE: 中间件连接串（PostgreSQL / Redis / Milvus / MinIO）将在下一个 PR 接入。
# 中间件默认凭据与端口见 docker/.env.example。
